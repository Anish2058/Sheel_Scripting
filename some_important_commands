#some daily used commands
ls,cp,mv,touch,grep,nano,cat, find 

#Simple Shell Script to list all processes

ps -ef | grep ubuntu | awk -F" " '{print $column_No.}'

#a script to print only errors from a remote log

curl "Url" | grep error
or, curl -X GET "URL" | grep error

curl,pipe,grep

#shell script to print number divisible by 3 & 5 and not 15

for i in {1..100}; do
        if (( i % 3 == 0 && i % 5 == 0 && i % 15 != 0 )); then
                echo "The numbers are $i";
        fi
done

#print odd,even, dividible by 3, prime number..... and others



#script to print numb er of S in mississippi
x=mississippi
grep -o "s" <<< "$x" | wc -l

#crontab -> linux admin cron -> time based job scheduler

# open file in read only mode
nano -r filename

#difference between softlink and hardlink
created a file and saved in memory
reuse copy as a hardlink directly point to the file data but softlink points to the file name

#break and continue

breaking the execution of program 
continue the execution of program(SKIP AND CONTINUE)

set -u -> until not use the shell script will not complain unused variable

#bash dynamically or statically typed
bash is dynamically type as we donot declare the variables 

#sort list of names in a file
Sort

#managing logs of system that generate huge log files everyday
logrotate
logrotate(gzip,zip,tar...)
